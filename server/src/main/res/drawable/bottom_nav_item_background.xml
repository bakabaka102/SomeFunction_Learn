<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#33C09CFF"> <!-- tím nhạt, 20% opacity -->

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" /> <!-- Bo tròn ripple -->
            <solid android:color="#ffffff" /> <!-- Mask là bắt buộc để giới hạn ripple -->
        </shape>
    </item>
</ripple>
